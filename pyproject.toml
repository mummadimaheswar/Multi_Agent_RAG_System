[tool.pytest.ini_options]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
testpaths = ["tests"]

[project]
name = "ai-agents"
version = "4.0.0"
description = "Multi-agent AI orchestrator with Travel, Financial, and Health agents"
requires-python = ">=3.11"
